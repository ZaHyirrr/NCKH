# Render Static Site: nckh-frontend
# Set this env var in Render dashboard.

# Include /api because backend runs with server.servlet.context-path=/api
VITE_API_URL=https://REPLACE_WITH_BACKEND_DOMAIN.onrender.com/api
